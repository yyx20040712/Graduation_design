# 项目进度总览 — 2026-05-16 → 2026-05-18

## 系统架构

```
main.py → bootstrap.extract_resources() → MainWindow (tkinter)
  ├── canvas_view.py      节点画布 (菜单自动生成)
  ├── solution_browser.py 方案浏览器 (自动发现)
  ├── graph_executor.py   DAG执行引擎
  ├── models/             16处理单元 (市政10+矿井水5+AAO1)
  ├── models/cost/        工程概算 (自动路由)
  ├── pipe_hydraulic.py   管网水力计算(曼宁)
  └── output_writer.py    分类Excel+安全系数

mods/ (模组系统 — 单文件夹注册)
  ├── mod_manager.py      扫描/加载/注册 + 自动发现引擎
  ├── core/               15 内置模组 (mod.json + discretization.json)
  └── community/          用户模组空间
```

## 数据流

```
方案浏览器(向量化) → 应用方案 → node._result(CLEAN)
     ↓
  F5跳过 → 导出直接用缓存
```

## 修复清单 (28项 + 7项新)

**数据流**: Combiner流量翻倍, CLEAN节点水质, remove_node split, F5覆盖缓存  
**约束**: UV N_rows, 砂斗容积, CASS B钳制, 堰长双侧, 约束名称对齐, D/h2/t/L/B/滤速/水深调整, 安全裕度加权  
**概算**: PIPE重复, node_type缺失, 池壁高度, 泵站顺序, 前期工作费  
**映射**: _val三级匹配, val_过滤, VEC_FIELD补全  
**UI**: 按钮溢出, 水质表空, 工具栏合并  
**管网**: ExcelWriter corruption → 独立输出文件

**🆕 打包 (2026-05-18)**:
- bootstrap.py 资源自解压模块
- _paths.py frozen 模式路径重定向
- 7 处 `__file__` → `_paths` 函数迁移
- PyInstaller spec 8 项资源嵌入
- 52MB 自包含 EXE 构建 + 集成测试通过

## 新增功能

- 📐 安全排序 + 📊 安全系数输出
- ✓ 已应用方案绿色高亮  
- 📏 污水/雨水水力计算
- 🎯 自动选择最安全方案
- 📦 **自解压 EXE 打包** — 52MB 单文件，首次运行释放 knowledge/output/mods/data/logs + 2md + 1json 案例

## 操作流程

```
1. 打开 → 📏水力计算 → _计算结果.xlsx
2. ▶F5 → 构筑物设计
3. 💰概算报告 → 📤全部输出
```

## 🆕 打包流程

```
1. 首次运行 EXE → 自动释放 resources 到当前目录
2. 用户可编辑 config.ini、添加 mods/community/、放 data/
3. 所有日志 → logs/ddesign_YYYYMMDD.log
4. 输出文件 → output/
```
